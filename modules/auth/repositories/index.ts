// Auth repositories — Better Auth manages auth tables directly
// Custom auth queries are handled by userRepository

export { userRepository } from './user-repository'
export type { IUserRepository } from './user-repository'
