/**
 * Auth Server Actions.
 *
 * Better Auth handles authentication flows (login, register, OAuth, magic links,
 * 2FA) directly via the client SDK (`authClient`). No server actions are needed
 * for those operations.
 *
 * Add server actions here ONLY for auth-adjacent operations that require
 * server-side logic beyond what Better Auth provides (e.g., custom session
 * enrichment, admin-only auth operations).
 */
